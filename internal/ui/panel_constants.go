package ui

// Panel layout constants define sizing and spacing for UI elements.
const (
	// BorderSize defines the width of panel borders (e.g., a single character for a rounded border).
	BorderSize = 1

	// PanelGap specifies the space between the file tree and preview panels when both are visible.
	PanelGap = 2
	// File tree panel header height
	FileTreePanelHeaderHeight = 1

	// FileTreePaddingL is the left padding within the file tree panel.
	FileTreePaddingL = 1

	// FileTreePaddingR is the right padding within the file tree panel.
	FileTreePaddingR = 1

	// PreviewContentPaddingL is the left padding for the content area within the preview panel.
	PreviewContentPaddingL = 1

	// PreviewContentPaddingR is the right padding for the content area within the preview panel.
	PreviewContentPaddingR = 1

	// MinInnerContentWidth is the minimum internal width for panel content to ensure readability.
	MinInnerContentWidth = 10

	// DefaultHeaderHeight is the standard height allocated for the header section of the UI.
	DefaultHeaderHeight = 1 // Adjusted from 2 to 1, as typical headers are single line.

	// DefaultFooterHeight is the standard height allocated for the footer section of the UI.
	DefaultFooterHeight = 1 // Adjusted from 2 to 1, as typical footers are single line.

	// PreviewHeaderHeight is the height allocated for the header within the preview panel.
	PreviewHeaderHeight = 1
)
